{
  "name": "ds-context-intelligence",
  "version": "1.0.0",
  "description": "Design System Context Intelligence for MCP",
  "main": "dist/code.js",
  "scripts": {
    "build": "npm run build:ui && npm run build:manifest",
    "build:manifest": "copy manifest.dist.json dist\\manifest.json",
    "build:ui": "node scripts/write-version.js && webpack --mode development",
    "clean": "if exist dist rmdir /s /q dist && mkdir dist",
    "rebuild": "npm run clean && npm run build",
    "watch": "webpack --mode development --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "css-loader": "^7.1.3",
    "style-loader": "^4.0.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@create-figma-plugin/ui": "^4.0.3",
    "preact": "^10.28.3"
  }
}
